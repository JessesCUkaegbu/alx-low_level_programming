#include "main.h"
/**
 * _strlen - returns the lenghth of a string
 * @s: String
 * Return: length
*/
void puts_half(char *str)
{
	int long1 = 0;

	while (*s != '\0')
	{
		long1++;
		s++;
	}

	return (long1);
}
