#include "main.h"

/**
 * add - Write a function that adds two integers and returns the result
 * add - Write a function that adds two integers and returns the result.
 * @x: first number to compute
 * @y: second number to compute
 *
 * Return: Always 0. (Success).
 */
int add(int x, int y)
{
	return (x + y);
}
